List<String> ePageTitle = ["home", "in list", "out list", "inven", "setting"];

enum PageType { Home, ProductIn, ProductOut, ProductInven, ProductRegIn, ProductRegOut, Setting }

enum FilterType { Name, Code, Element, Company, Barcode }
